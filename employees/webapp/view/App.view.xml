<mvc:View controllerName="hcm.employees.controller.App"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m">

    <Shell id= "shell">  <!-- Contenedor principal -->
        <App id="app">   <!-- Permite navegar entre vistas -->
            <pages>
                <Page id="page" title="{i18n>title}">
                    <content>
                        <!-- <Label id="label1" text="{i18n>text1}" />
                        <Input id="input1" value="Prueba" /> -->

                        <mvc:XMLView    id="viewPanel"
                                        viewName="hcm.employees.view.Panel"
                                        type="XML"/>

                        <mvc:XMLView    id="invoicesList"
                                        viewName="hcm.employees.view.invoicesList"
                                        type="XML"/>

                        
                    </content>
                </Page>
            </pages>        
        </App>
    </Shell>

</mvc:View>
